services:
  mongo:
    image: mongo:latest
    container_name: mongo_container
    ports: 
      - 27017:27017
    command: ["--replSet", "rs0", "--bind_ip_all"]  
    volumes:
      - db-data:/data/db

volumes:
  db-data:
    driver: local

# mongosh

# use admin

# rs.initiate()

# db.createUser({
#   user: "admin",
#   pwd: "password", 
#   roles: [ { role: "root", db: "admin" } ]
# })

# cfg = rs.conf()
# cfg.members[0].host = "127.0.0.1:27017"
# rs.reconfig(cfg, { force: true })


# mongodb://admin:password@127.0.0.1:27017/?replicaSet=rs0&authSource=admin